

V10 UI-ändring: toppanelen visar inte längre Score eller avståndet till föregående dags Range High/Low. Score visas i Signalmodell-panelen.

## V11
- Fixed dashboard JavaScript after removing the Range High/Low distance cards.
- Removed stale references to `dist_rh` and `dist_rl` that caused the page to show `Datafel` after a successful API response.
- Improved API error handling so HTTP/JSON errors are shown more clearly.
